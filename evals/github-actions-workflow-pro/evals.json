{
  "skill_name": "github-actions-workflow-pro",
  "evals": [
    {
      "id": 0,
      "prompt": "Create a GitHub Actions CI workflow for a Node.js project. It should run on pull requests and pushes to main, install with npm ci, and run npm test. Save the workflow as ci.yml.",
      "expected_output": "A maintainable CI workflow that uses explicit read permissions, concurrency cancellation, npm caching, a timeout, and clear job/step names.",
      "files": [],
      "expectations": [
        "The workflow runs `npm ci` as the install step and `npm test` as the test step, as the prompt asked; a substituted command (`npm install`, `npm run test:ci`) fails this.",
        "The workflow sets explicit least-privilege permissions rather than relying on GitHub defaults.",
        "The workflow defines concurrency with cancel-in-progress for superseded branch or PR runs.",
        "The Node setup uses npm caching or an equivalent package-manager cache.",
        "The workflow has a top-level `name:`; every job has `name:` and `timeout-minutes:`; every step that uses an action has `name:`.",
        "The produced or corrected workflow YAML passes actionlint, zizmor (regular persona), poutine, and `pinact run -check -verify-comment -min-age 7 -verify-min-age` with zero findings when the grader runs them on the output, except poutine `default_permissions_on_risky_events` on a job whose `permissions: {}` is explicit (known false positive: the rule counts `{}` as unset), and zizmor `dangerous-triggers` when the output keeps `pull_request_target` and the answer states why (Collaborators-only PR access, or private repo with the split-job pattern).",
        "The hand-back explains, in one line each, the reason for every opinionated default it applied.",
        "The answer asks only about decisions the repo cannot answer (or states none were needed); it does not ask the user for facts it could look up.",
        "Every commit SHA that is new in a `uses:` line (not present in the input files) was written by `pinact run` on the file: the transcript shows the `pinact run` command, and shows no `gh api` call (`git/refs/tags`, `/commits/`, `/releases`), `git ls-remote`, or copy of a SHA from another file that then went into the YAML. A run that wrote no new SHA passes this assertion."
      ]
    },
    {
      "id": 1,
      "prompt": "Add a workflow that builds my Docker image on PRs and publishes it to ghcr.io on main and semver tags. Use the repository name for the image. Save it as docker.yml.",
      "expected_output": "A Docker workflow that builds without pushing on PRs, publishes to GHCR on trusted refs, uses Buildx, metadata-action, build-push-action, GHA cache, scoped package permissions, and provenance/SBOM.",
      "files": [],
      "expectations": [
        "The workflow builds on pull requests and pushes to main and semver-style tags.",
        "Image push, provenance, and SBOM happen only on trusted refs (push to the default branch or a semver tag). The pull_request path builds with push: false in a job that holds no packages: write; the job that pushes is the only one granted packages: write and does not run on pull_request events. A single job with `push: ${{ github.event_name != 'pull_request' }}` and packages: write on every event fails this.",
        "The image publishing job grants packages: write without granting broad write-all permissions.",
        "The workflow uses Buildx cache and enables provenance and SBOM for published images.",
        "The produced or corrected workflow YAML passes actionlint, zizmor (regular persona), poutine, and `pinact run -check -verify-comment -min-age 7 -verify-min-age` with zero findings when the grader runs them on the output, except poutine `default_permissions_on_risky_events` on a job whose `permissions: {}` is explicit (known false positive: the rule counts `{}` as unset), and zizmor `dangerous-triggers` when the output keeps `pull_request_target` and the answer states why (Collaborators-only PR access, or private repo with the split-job pattern).",
        "The hand-back explains, in one line each, the reason for every opinionated default it applied.",
        "The workflow is saved as docker.yml and the image name derives from the repository (`${{ github.repository }}` or `ghcr.io/${{ github.repository }}` in metadata-action `images:`), as the prompt asked.",
        "Every commit SHA that is new in a `uses:` line (not present in the input files) was written by `pinact run` on the file: the transcript shows the `pinact run` command, and shows no `gh api` call (`git/refs/tags`, `/commits/`, `/releases`), `git ls-remote`, or copy of a SHA from another file that then went into the YAML. A run that wrote no new SHA passes this assertion."
      ]
    },
    {
      "id": 2,
      "prompt": "Review and improve this workflow for safety and speed: it uses pull_request_target, permissions: write-all, docker build && docker push in a shell step, and AWS_ACCESS_KEY_ID/AWS_SECRET_ACCESS_KEY secrets to deploy. Produce the corrected workflow and explain the important changes.",
      "expected_output": "A hardened rewrite that avoids dangerous pull_request_target behavior, removes write-all permissions, uses Docker actions instead of shell push logic, recommends OIDC for AWS auth, and includes speed/maintainability improvements.",
      "files": [],
      "expectations": [
        "The answer identifies pull_request_target with privileged operations as risky and avoids or constrains it.",
        "The corrected workflow removes permissions: write-all and uses explicit least-privilege permissions.",
        "The corrected workflow replaces raw docker build and docker push shell commands with Buildx-oriented Docker actions.",
        "The answer recommends or implements AWS OIDC instead of long-lived AWS access key secrets.",
        "Every job has `timeout-minutes:`; a `concurrency:` block exists at the workflow or job level; the build caches (`cache:` on a setup action, or `cache-from: type=gha` on build-push-action).",
        "The produced or corrected workflow YAML passes actionlint, zizmor (regular persona), poutine, and `pinact run -check -verify-comment -min-age 7 -verify-min-age` with zero findings when the grader runs them on the output, except poutine `default_permissions_on_risky_events` on a job whose `permissions: {}` is explicit (known false positive: the rule counts `{}` as unset), and zizmor `dangerous-triggers` when the output keeps `pull_request_target` and the answer states why (Collaborators-only PR access, or private repo with the split-job pattern).",
        "The hand-back explains, in one line each, the reason for every opinionated default it applied.",
        "Every placeholder the answer introduced (role ARN, region, registry, environment name) is listed for the user to confirm.",
        "Image push and deploy run only on push to the default branch or on tags; the pull_request path builds with push: false and holds neither packages: write nor deploy credentials (no id-token: write, no AWS role).",
        "The deploy step consumes the image by the digest the build job output (docker/build-push-action `outputs.digest` passed through job outputs), not by a mutable tag.",
        "Every commit SHA that is new in a `uses:` line (not present in the input files) was written by `pinact run` on the file: the transcript shows the `pinact run` command, and shows no `gh api` call (`git/refs/tags`, `/commits/`, `/releases`), `git ls-remote`, or copy of a SHA from another file that then went into the YAML. A run that wrote no new SHA passes this assertion."
      ]
    },
    {
      "id": 3,
      "prompt": "Audit this workflow (.github/workflows/ci.yml, attached) for security problems. Give me a report of what is wrong and a corrected version.",
      "expected_output": "An audit report that separates hard findings from opinions, identifies pull_request_target with untrusted checkout, write-all permissions, unpinned actions, a version comment carrying extra text, template injection of the PR title, and a corrected workflow that passes actionlint and zizmor.",
      "files": ["evals/github-actions-workflow-pro/fixtures/insecure-ci.yml"],
      "expectations": [
        "The report identifies pull_request_target combined with checking out the PR head as a critical risk and moves the workflow to pull_request or isolates the untrusted checkout.",
        "The report identifies the PR title interpolated directly into a run: block as template injection, and the corrected workflow no longer interpolates it into a shell (passed through env, or the step removed).",
        "The corrected workflow replaces permissions: write-all with permissions: {} at the top and explicit per-job grants.",
        "The corrected workflow pins actions/checkout and actions/setup-node to full commit SHAs with a version comment.",
        "The report has the two headings from the audit.md template as their own `##` headings: `Hard findings` for tool- or rule-backed items and `Opinions` for style items.",
        "Under a `## Tools` heading, the report lists actionlint, shellcheck, zizmor, poutine, pinact, gasa, and run-stats, each marked ran, absent, or skipped: <reason>; a sentence that only says the scanners were run fails this.",
        "The produced or corrected workflow YAML passes actionlint, zizmor (regular persona), poutine, and `pinact run -check -verify-comment -min-age 7 -verify-min-age` with zero findings when the grader runs them on the output, except poutine `default_permissions_on_risky_events` on a job whose `permissions: {}` is explicit (known false positive: the rule counts `{}` as unset), and zizmor `dangerous-triggers` when the output keeps `pull_request_target` and the answer states why (Collaborators-only PR access, or private repo with the split-job pattern).",
        "The corrected workflow still does what the original did: tests run on the same events, and the PR comment step is kept in a safe shape (env-passed values, no pull_request_target) or its removal is stated with the reason. Correctness names the step that can never succeed as written: `gh pr comment` also runs on `push` events, where there is no PR number.",
        "If pinact moved an action to a newer major than the original (`@v4` becomes a `# v7.x` comment), the hand-back names each such action and gives the way to keep the old major: write the tag in the file (`actions/checkout@v4`) and run `scripts/scan.sh pinact run -min-age 7 -i 'actions/checkout' <file>` without `-update`; silent major jumps fail this, and so does quoting `-i 'actions/checkout@v4'` (pinact's `-i` is a regex over the action name, so that matches nothing).",
        "The report flags the upload-artifact line whose version comment carries extra text after the version (Dependabot skips updating a comment with anything besides the version, so the pin silently goes stale), and in the corrected file that comment is the version alone.",
        "Every commit SHA that is new in a `uses:` line (not present in the input files) was written by `pinact run` on the file: the transcript shows the `pinact run` command, and shows no `gh api` call (`git/refs/tags`, `/commits/`, `/releases`), `git ls-remote`, or copy of a SHA from another file that then went into the YAML. A run that wrote no new SHA passes this assertion."
      ]
    },
    {
      "id": 4,
      "prompt": "Our CI (.github/workflows/ci.yml, attached) takes about 25 minutes on every push and people have started ignoring it. Speed it up without dropping real coverage.",
      "expected_output": "A faster workflow that adds concurrency cancellation, npm caching, timeouts, drops the redundant fetch-depth: 0 and duplicate matrix cell, runs lint and test in parallel, adds Buildx cache for the image build, keeps the existing pins and permissions, and explains why each change helps.",
      "files": ["evals/github-actions-workflow-pro/fixtures/slow-ci.yml"],
      "expectations": [
        "The workflow gains a concurrency block with cancel-in-progress for superseded runs.",
        "setup-node gains cache: npm or an equivalent package-manager cache.",
        "Only the redundant matrix alias is removed: ubuntu-latest and ubuntu-24.04 are the same runner today, so one of them goes; ubuntu-22.04 is a distinct runner and stays, or its removal is proposed as a coverage question with the reason.",
        "fetch-depth: 0 is removed from jobs that do not need history, or its removal is proposed with the reason.",
        "Lint and test no longer run strictly sequentially, or the answer justifies keeping needs: lint.",
        "Jobs gain timeout-minutes.",
        "The docker build step moves to docker/setup-buildx-action and docker/build-push-action with type=gha cache, or the answer recommends that change.",
        "The existing SHA pins are preserved exactly (same SHAs and version comments, including setup-node `# v4.4.0`; a whole-file `pinact -update` would have bumped it) and the permissions block is not loosened.",
        "Each speed change is explained with the reason it saves time.",
        "The produced or corrected workflow YAML passes actionlint, zizmor (regular persona), poutine, and `pinact run -check -verify-comment -min-age 7 -verify-min-age` with zero findings when the grader runs them on the output, except poutine `default_permissions_on_risky_events` on a job whose `permissions: {}` is explicit (known false positive: the rule counts `{}` as unset), and zizmor `dangerous-triggers` when the output keeps `pull_request_target` and the answer states why (Collaborators-only PR access, or private repo with the split-job pattern).",
        "The `on:` block keeps the original events and branch filters (the prompt asked for speed without dropping coverage); narrowing `push` to `branches: [main]` or dropping an event fails this unless the answer proposes it as a separate question that names the coverage it drops.",
        "Every commit SHA that is new in a `uses:` line (not present in the input files) was written by `pinact run` on the file: the transcript shows the `pinact run` command, and shows no `gh api` call (`git/refs/tags`, `/commits/`, `/releases`), `git ls-remote`, or copy of a SHA from another file that then went into the YAML. A run that wrote no new SHA passes this assertion."
      ]
    },
    {
      "id": 5,
      "prompt": "Add a job to my CI workflow (.github/workflows/ci.yml, attached) that runs `npm run lint`. Keep everything else as it is.",
      "expected_output": "The same workflow with one new lint job that matches the existing job's conventions: permissions block, same pinned checkout and setup-node with version comments, persist-credentials: false, npm cache, timeout, friendly name, running in parallel with the test job. No unrelated changes. The one tag-pinned action already in the file (actions/upload-artifact@v4) is SHA-pinned with a scoped pinact run; nothing else in the test job changes.",
      "files": [
        "evals/github-actions-workflow-pro/fixtures/good-ci-tagged.yml"
      ],
      "expectations": [
        "The new lint job has its own permissions block with contents: read.",
        "The new lint job uses the same full-SHA pins and version comments for actions/checkout and actions/setup-node as the existing test job.",
        "The new lint job sets persist-credentials: false on checkout and cache: npm on setup-node, matching the existing job.",
        "The new lint job has timeout-minutes and a name a person would search for in the Actions tab (capitalized words with spaces, such as `Lint`); a filename, an id-style name (`lint-1`), or no name fails this.",
        "The lint job runs in parallel with the test job rather than being chained with needs, or the answer justifies chaining.",
        "The existing test job, triggers, permissions, and concurrency block are unchanged byte for byte except the `actions/upload-artifact` line, including the setup-node SHA and `# v4.4.0` comment (a whole-file `pinact -update` would have bumped it).",
        "No path filters, workflow_dispatch, or other unrequested triggers are added.",
        "The produced or corrected workflow YAML passes actionlint, zizmor (regular persona), poutine, and `pinact run -check -verify-comment -min-age 7 -verify-min-age` with zero findings when the grader runs them on the output, except poutine `default_permissions_on_risky_events` on a job whose `permissions: {}` is explicit (known false positive: the rule counts `{}` as unset), and zizmor `dangerous-triggers` when the output keeps `pull_request_target` and the answer states why (Collaborators-only PR access, or private repo with the split-job pattern).",
        "The lint job runs `npm ci` and then `npm run lint`; a copied test job that still runs `npm test`, or a lint step without a preceding install, fails this.",
        "The pre-existing `actions/upload-artifact@v4` line is SHA-pinned with a version comment in the output (the skill pins every tag it touches, scoped with `-i` so nothing else moves); a tag left in place fails this.",
        "Every commit SHA that is new in a `uses:` line (not present in the input files) was written by `pinact run` on the file: the transcript shows the `pinact run` command, and shows no `gh api` call (`git/refs/tags`, `/commits/`, `/releases`), `git ls-remote`, or copy of a SHA from another file that then went into the YAML. A run that wrote no new SHA passes this assertion."
      ]
    },
    {
      "id": 6,
      "prompt": "Audit all the GitHub Actions workflows in this repo. Security, speed, anything broken. Prioritize what I should fix first.",
      "expected_output": "A full-repo audit that runs the scanners and cites their rule ids, marks run-stats and gasa skipped because the repo has no remote, opens with a Do-first list of at most five items, has Correctness/Hard/Speed/Opinions sections, asks (or states the default for) the delivery format, and does not edit the files.",
      "files": [
        "evals/github-actions-workflow-pro/fixtures/insecure-ci.yml",
        "evals/github-actions-workflow-pro/fixtures/slow-ci.yml",
        "evals/github-actions-workflow-pro/fixtures/deploy-static-keys.yml"
      ],
      "expectations": [
        "The report opens with a Do-first list of one to five items, ordered with the most severe first.",
        "Every tool-detected finding cites the tool and its rule id (for example zizmor template-injection, gasa workflows/action-version-pinning, poutine untrusted_checkout_exec) rather than restating the rule in the agent's own words.",
        "The report has the four headings from the audit.md template, each as its own `##` heading: `Correctness`, `Hard findings`, `Speed`, `Opinions`.",
        "run-stats and gasa are marked skipped with the reason (no GitHub remote), and the Tools section lists each scanner as ran or 'absent'.",
        "The report asks how the user wants it delivered (chat, HTML dashboard, Markdown file, or combination) or explicitly states it defaulted to chat because the user was unavailable.",
        "The speed section covers all three workflows and names the deploy workflow's cancel-in-progress concurrency as a risk for deploys.",
        "The workflow files in the repo are left unmodified; fixes appear as a proposed diff.",
        "The proposed corrected YAML passes actionlint, zizmor (regular persona), poutine, and `pinact run -check -verify-comment -min-age 7 -verify-min-age` with zero findings when the grader runs them on the output, except poutine `default_permissions_on_risky_events` on a job whose `permissions: {}` is explicit (known false positive: the rule counts `{}` as unset), and zizmor `dangerous-triggers` when the output keeps `pull_request_target` and the answer states why (Collaborators-only PR access, or private repo with the split-job pattern).",
        "Lines that were already SHA-pinned in the originals (deploy-static-keys.yml and slow-ci.yml: checkout `# v7.0.1`, setup-node `# v4.4.0`) keep their SHA and version comment in the proposed YAML, or move forward with the reason stated; a pin copied from another file's pinact output onto these lines (a downgrade to `# v4.4.0`) is a hand lookup and fails this.",
        "Every finding the proposed diff fixes appears in the report under the section it belongs to (an actionlint SC2086 quoting fix in the diff with Correctness saying 'none found' fails this).",
        "Each proposed workflow still does what the original did (deploy still deploys, tests still run, the PR comment is kept in a safe shape or its removal is stated), and Correctness names the step that can never succeed as written: `gh pr comment` also runs on `push` events, where there is no PR number.",
        "Every commit SHA that is new in a `uses:` line (not present in the input files) was written by `pinact run` on the file: the transcript shows the `pinact run` command, and shows no `gh api` call (`git/refs/tags`, `/commits/`, `/releases`), `git ls-remote`, or copy of a SHA from another file that then went into the YAML. A run that wrote no new SHA passes this assertion."
      ]
    },
    {
      "id": 7,
      "prompt": "Harden my deploy workflow (.github/workflows/deploy.yml, attached). We deploy a static site to S3 and CloudFront from main.",
      "expected_output": "A hardened deploy workflow using OIDC instead of static AWS keys, a non-cancelling concurrency group, an environment scoping the secrets, set -euo pipefail in the multi-line run block, persist-credentials: false, a timeout, an offer of the Dependabot snippet, and placeholders listed.",
      "files": [
        "evals/github-actions-workflow-pro/fixtures/deploy-static-keys.yml"
      ],
      "expectations": [
        "The corrected workflow replaces AWS_ACCESS_KEY_ID/AWS_SECRET_ACCESS_KEY with aws-actions/configure-aws-credentials using role-to-assume and id-token: write.",
        "The deploy job's concurrency block has `cancel-in-progress: false` (or a queue group with no cancel).",
        "The answer gives the reason the deploy concurrency does not cancel in-progress runs (a cancelled deploy can leave the target half-updated).",
        "The deploy job declares an environment and the remaining secrets are scoped to it.",
        "Any multi-line run block that remains starts with set -euo pipefail (splitting into single-line steps, which fail fast on their own, also satisfies this).",
        "actions/checkout sets persist-credentials: false and the job has timeout-minutes.",
        "The answer recommends a Dependabot github-actions entry (daily, cooldown 7) because none exists, as a complete file.",
        "Every placeholder introduced (role ARN, environment name, region) is listed for the user to confirm.",
        "The produced or corrected workflow YAML passes actionlint, zizmor (regular persona), poutine, and `pinact run -check -verify-comment -min-age 7 -verify-min-age` with zero findings when the grader runs them on the output, except poutine `default_permissions_on_risky_events` on a job whose `permissions: {}` is explicit (known false positive: the rule counts `{}` as unset), and zizmor `dangerous-triggers` when the output keeps `pull_request_target` and the answer states why (Collaborators-only PR access, or private repo with the split-job pattern).",
        "aws-actions/configure-aws-credentials runs after `npm ci` and `npm run build`, so package lifecycle scripts never run with cloud credentials in the environment; or the build runs in a separate job that has no id-token: write and hands the artifact to the deploy job.",
        "The workflow's top-level `permissions:` is `{}` and the deploy job's own `permissions:` grants `contents: read` and `id-token: write`.",
        "The report lists the original's top-level `contents: read` block under `## Hard findings` with the reason (a non-empty top-level grant applies to every job in the file).",
        "Every commit SHA that is new in a `uses:` line (not present in the input files) was written by `pinact run` on the file: the transcript shows the `pinact run` command, and shows no `gh api` call (`git/refs/tags`, `/commits/`, `/releases`), `git ls-remote`, or copy of a SHA from another file that then went into the YAML. A run that wrote no new SHA passes this assertion."
      ]
    },
    {
      "id": 8,
      "prompt": "Set up CI and container image publishing for this repo (it's a Node app with a Dockerfile). Use GitHub Actions.",
      "expected_output": "ci.yml and docker.yml that read the Node version from .nvmrc, run lint and test in parallel, publish to ghcr.io only on trusted refs, offer the reusable docker-build-workflow before writing a bespoke one, ask only about decisions the repo cannot answer, and add no unrequested triggers.",
      "files": [
        "evals/github-actions-workflow-pro/fixtures/node-repo/.nvmrc",
        "evals/github-actions-workflow-pro/fixtures/node-repo/package.json",
        "evals/github-actions-workflow-pro/fixtures/node-repo/package-lock.json",
        "evals/github-actions-workflow-pro/fixtures/node-repo/Dockerfile"
      ],
      "expectations": [
        "setup-node uses node-version-file: .nvmrc rather than a hardcoded version.",
        "Lint and test run as parallel jobs using the repo's own npm scripts; the reusable super-linter workflow is not proposed because the repo has its own linter.",
        "The answer names `BretFisher/docker-build-workflow` as a reusable alternative before or alongside the bespoke docker.yml.",
        "The answer asks whether the user already uses a reusable docker build workflow, or states it took the bespoke default because it could not ask.",
        "The answer asks only about decisions the repo cannot answer (registry, whether to publish on tags) and does not ask for facts it could infer.",
        "No workflow_dispatch, repository_dispatch, or path filters are added.",
        "The hand-back gives a one-line reason for each of these defaults it applied: least-privilege permissions, SHA pinning, concurrency, caching, timeout-minutes, and trusted-refs publishing (PR builds do not push). Other defaults may go unexplained.",
        "The produced or corrected workflow YAML passes actionlint, zizmor (regular persona), poutine, and `pinact run -check -verify-comment -min-age 7 -verify-min-age` with zero findings when the grader runs them on the output, except poutine `default_permissions_on_risky_events` on a job whose `permissions: {}` is explicit (known false positive: the rule counts `{}` as unset), and zizmor `dangerous-triggers` when the output keeps `pull_request_target` and the answer states why (Collaborators-only PR access, or private repo with the split-job pattern).",
        "Image push, provenance, and SBOM happen only on trusted refs (push to the default branch or a semver tag). The pull_request path builds with push: false in a job that holds no packages: write; the job that pushes is the only one granted packages: write and does not run on pull_request events. A single job with `push: ${{ github.event_name != 'pull_request' }}` and packages: write on every event fails this.",
        "Every commit SHA that is new in a `uses:` line (not present in the input files) was written by `pinact run` on the file: the transcript shows the `pinact run` command, and shows no `gh api` call (`git/refs/tags`, `/commits/`, `/releases`), `git ls-remote`, or copy of a SHA from another file that then went into the YAML. A run that wrote no new SHA passes this assertion."
      ]
    },
    {
      "id": 9,
      "prompt": "Audit all the GitHub Actions workflows in this repo. Security, speed, anything outdated.",
      "expected_output": "An audit that recognizes link-checker.md + link-checker.lock.yml as a compiled gh-aw agentic workflow pair, gives it its own section where staleness is the finding (compiler v0.79.0 behind the latest gh-aw release), recommends gh aw update-actions then gh aw compile rather than editing the lock or pointing Dependabot at it, and audits good-ci.yml as a normal workflow.",
      "files": [
        "evals/github-actions-workflow-pro/fixtures/link-checker.md",
        "evals/github-actions-workflow-pro/fixtures/link-checker.lock.yml",
        "evals/github-actions-workflow-pro/fixtures/good-ci.yml",
        "evals/github-actions-workflow-pro/fixtures/dependabot-actions.yml",
        "evals/github-actions-workflow-pro/fixtures/node-repo/.nvmrc",
        "evals/github-actions-workflow-pro/fixtures/node-repo/package.json"
      ],
      "expectations": [
        "The report identifies link-checker.lock.yml and link-checker.md as a compiled GitHub Agentic Workflow (gh-aw) pair and gives the pair its own section, separate from the normal workflow findings.",
        "The lock file is never treated as hand-editable: no proposed diff or corrected YAML modifies link-checker.lock.yml, and any change to the pair is directed at link-checker.md followed by `gh aw compile`.",
        "The report warns the pair is stale: it reads the compiler version (v0.79.0) from the lock file's generated-by header, compares it with the latest gh-aw release, and flags the lock as compiled by an outdated compiler (a check of the lock's last commit date also satisfies the age half).",
        "The recommended remediation names both commands, `gh aw update-actions` (refreshing .github/aw/actions-lock.json) and `gh aw compile`, committing both.",
        "The report does not propose Dependabot or Renovate updates inside the lock file, and states why (the file is generated as one unit; Dependabot PRs against the lock are closed, not merged), while a repo `dependabot.yml` for normal workflows stays correct.",
        "Any scanner finding cited against link-checker.lock.yml appears in the pair's section with recompilation as the fix, not under Hard findings as a hand-edit.",
        "good-ci.yml is audited as a normal workflow and appears in the report outside the agentic pair's section.",
        "Any proposed corrected YAML (there may be none, since good-ci.yml is already clean) passes actionlint, zizmor (regular persona), poutine, and `pinact run -check -verify-comment -min-age 7 -verify-min-age` with zero findings when the grader runs them on the output, except poutine `default_permissions_on_risky_events` on a job whose `permissions: {}` is explicit (known false positive: the rule counts `{}` as unset), and zizmor `dangerous-triggers` when the output keeps `pull_request_target` and the answer states why (Collaborators-only PR access, or private repo with the split-job pattern).",
        "Every commit SHA that is new in a `uses:` line (not present in the input files) was written by `pinact run` on the file: the transcript shows the `pinact run` command, and shows no `gh api` call (`git/refs/tags`, `/commits/`, `/releases`), `git ls-remote`, or copy of a SHA from another file that then went into the YAML. A run that wrote no new SHA passes this assertion."
      ]
    },
    {
      "id": 10,
      "prompt": "Our build job (.github/workflows/build.yml, attached) takes about 11 minutes and most of it is the three builds running one after another. There's also an API server we start with `&` and a hardcoded `sleep 30` before the e2e tests, which is flaky. Can you speed this up? Keeping it as one job matters to us \u2014 the builds share the one npm ci and the same workspace.",
      "expected_output": "The three independent builds run at the same time inside the one job with a `parallel:` block (or three `background: true` steps plus a `wait`), and the API server becomes a `background: true` step with an `id`, a readiness check instead of `sleep 30`, and a `cancel:` step after the e2e tests. The job stays one job. The hand-back says the keys are new (June 2026, github.com and GHEC) and that actionlint's schema does not know them yet, so its unknown-key reports on those lines are expected.",
      "files": [
        "evals/github-actions-workflow-pro/fixtures/serial-build-job.yml",
        "evals/github-actions-workflow-pro/fixtures/node-repo/.nvmrc",
        "evals/github-actions-workflow-pro/fixtures/node-repo/package.json"
      ],
      "expectations": [
        "The three build steps (Build Frontend, Build Backend, Build Docs) run concurrently inside the same job, using a `parallel:` block or three steps with `background: true` followed by a `wait`.",
        "The API server step becomes a background step (`background: true`) with an `id`, and the shell backgrounding (`npm run start:api &`) is gone from the produced YAML.",
        "The server is stopped with a `cancel:` step that names the server step's id, rather than being left running to the end of the job.",
        "The `sleep 30` is replaced by a readiness check (polling the server's port or health endpoint) before the e2e step, and the answer says why a fixed sleep is both slower and flakier.",
        "The work stays in one job: the produced workflow does not split the builds into separate jobs or a matrix, which would repeat the checkout and the npm ci.",
        "The hand-back tells the user that actionlint does not know the new step keys yet, so its `unexpected key \"background\"` / `step must run script with \"run\"` reports on those lines are the linter's stale schema and not workflow errors.",
        "The answer states that these step keys are a recent GitHub Actions feature (June 2026) rather than presenting them as long-standing syntax, and does not claim that steps inside a job cannot run in parallel.",
        "The produced YAML passes zizmor (regular persona), poutine, and `pinact run -check -verify-comment -verify-min-age`, and the only actionlint reports are the unknown-key ones on the new parallel-step keys.",
        "Every third-party `uses:` in the produced YAML is a full commit SHA with a version comment, and the SHAs that were already in the attached file are unchanged."
      ]
    },
    {
      "id": 11,
      "prompt": "CI is dragging \u2014 every push takes ~25 minutes. I ran your run-stats script and saved the output (run-stats-long-tail.md, attached) along with the workflow itself (.github/workflows/ci.yml). Where is the time actually going and what should I fix first?",
      "expected_output": "A speed analysis driven by the step table: every step at or over 2 minutes gets a named cause and fix (no npm cache on either Install Dependencies step, plain `docker build` with no Buildx layer cache, a whole coverage directory uploaded, an integration suite whose cause is not visible in the YAML), `Set up job` is called out as runner startup with no YAML fix, and the user is asked which duration cut they want for the rest of the analysis (every step over 1 minute, 30 seconds, or only over 5) instead of the report stopping at the default 2 minutes.",
      "files": [
        "evals/github-actions-workflow-pro/fixtures/run-stats-long-tail.md",
        "evals/github-actions-workflow-pro/fixtures/long-tail-ci.yml",
        "evals/github-actions-workflow-pro/fixtures/node-repo/.nvmrc",
        "evals/github-actions-workflow-pro/fixtures/node-repo/package.json"
      ],
      "expectations": [
        "All six steps flagged at or over 2 minutes are addressed by name: Run Integration Suite, Build And Push Image, both Install Dependencies steps, Set up job, and Upload Coverage.",
        "The threshold is put to the user: the report either asks which duration cut they want for the rest of the speed work, naming at least one concrete alternative (every step over 1 minute, over 30 seconds, or only over 5), or states that the 2-minute default was used and is theirs to change. A report that never mentions the cut fails.",
        "`Set up job` is identified as runner startup or queue time rather than a step in the YAML, and no YAML change is proposed for it.",
        "The fix for both Install Dependencies steps is the setup-node lockfile cache (`cache: npm` on `actions/setup-node`), not a hand-written `actions/cache` key.",
        "The fix for Build And Push Image names Buildx with a GitHub Actions layer cache (`docker/setup-buildx-action` plus `cache-from: type=gha` / `cache-to: type=gha,mode=max`) instead of the plain `docker build`.",
        "Run Integration Suite is treated honestly: the answer says the 9-minute mean has no cause visible in the YAML and offers to look at the run log or proposes a concrete mechanism (sharding across a matrix, or overlapping steps inside the job), rather than inventing a cause.",
        "The report ranks what to fix first by the time it would save, so the two Install Dependencies steps and the image build lead over the 2m05s Upload Coverage step.",
        "The workflow's triggers are preserved: no `paths-ignore` or narrowed `push` branches is applied silently as a speed fix, and any such suggestion is offered as a question that names what it would stop running.",
        "Any proposed YAML passes actionlint, zizmor (regular persona), poutine, and `pinact run -check -verify-comment -verify-min-age` with zero findings.",
        "The SHA pins already in the attached file are unchanged, and every commit SHA that is new in a `uses:` line was produced by `pinact run` rather than typed from memory or read from a `gh api` / `git ls-remote` lookup."
      ]
    }
  ]
}
