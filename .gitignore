# Skill eval run artifacts — transcripts, gradings, timings, benchmarks, viewer logs.
# Regenerated on every eval run and machine-specific, so they are not source of truth.
# The eval *definitions* (skills/<name>/evals/evals.json) ARE committed — only runs are ignored.
.evals/

# macOS Finder metadata
.DS_Store
